
# MyUtils - 个人多功能CLI工具箱

这是一个为了学习 Python 工程化、打包与 CLI 工具开发的练习项目。

通过这个项目，学习如何将多个独立的 Python 脚本（如文件哈希、IP查询等）整合到一个专业的、可通过 `pip install` 安装的命令行工具包中。

---

## 🚀 功能列表

- `myutils hello`: 打印 "Hello, World!"，测试工具是否正常工作。
- `myutils hash <文件路径> [-a 算法]`: 计算指定文件的哈希值 (默认 sha256)。
- `myutils tree [路径] [-d 深度]`: 以树状结构显示目录内容 (默认深度 2)。
- `myutils ip <IP地址>`: 查询指定 IP 地址的地理位置信息。

---

## 🛠️ 安装方法

1.  克隆本仓库:
    ```bash
    git clone [请在这里粘贴你自己的 GitHub 仓库 URL]
    cd myutils-project
    ```
   

2.  创建并激活 `venv` 虚拟环境 (推荐):
    ```bash
    # (Windows)
    python -m venv venv
    .\venv\Scripts\activate
    
    # (macOS/Linux)
    python3 -m venv venv
    source venv/bin/activate
    ```
   

3.  以“可编辑模式”安装所有依赖和工具:
    ```bash
    pip install -e .
    ```
   

---

## 💡 使用示例

### 1. 查询 IP 地址

```bash
(venv) $ myutils ip 8.8.8.8
--- IP 8.8.8.8 的信息 ---
  国家: United States
  城市: Virginia, Ashburn
  ISP : Google LLC
```

_(注意：你看到的城市可能与此不同，API 结果会变化)_

### 2. 显示目录树


```Bash
(venv) $ myutils tree myutils -d 1
├── __pycache__
├── __init__.py
├── cli.py
├── file_tools.py
└── web_tools.py
```

### 3. 计算文件哈希


```Bash
(venv) $ myutils hash pyproject.toml
d48797b9e61e9a20bebc365f9a9d8dd617d517e22b21d58a01a509239a366bc8
```

